syntax="proto3";
package platform.user;

option go_package = "platform/user";

enum Gender {
    option allow_alias = true;
    Unknown = 0;
    ProductMale = 1;
    Female = 2;
}

enum ActiveType {
    option allow_alias = true;
    Unknown = 0;
    Email = 1;
    SMSCode = 2;
}
